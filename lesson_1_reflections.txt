How did viewing a diff between two versions of a file help you see the bug that
was introduced?

* Reducing the problem to the lines changed.

How could having easy access to the entire history of a file make you a more 
efficient programmer in the long term?

* It allows to come back to any past version and view the changes since then.

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
Docs does?

* pros: I choose when to commit
* cons: I decide when to commit

Why do you think some version control systems, like Git, allow saving multiple 
files in one commit, while others, like Google Docs, treat each file 
separately?

* It makes more sense to save multiple files at once when we talk about code.

How can you use the commands git log and git diff to view the history of files?

* git log to get the commit id and then git diff to view the changes made to
the code from a previous version.

How might using version control make you more confident to make changes that
could break something?

* Because it allows you to get back to any version you might made before.

Now that you haver your workspace set up, what do you want to try using Git for?

* To take care of my text files: code, notes, ...
